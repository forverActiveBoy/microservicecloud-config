spring:
  profiles:
    active: 
    - dev
---
# 开发配置
server
  port: 8081
spring:
  application:
    name:  microservicecloud-config-dev
  profiles: dev


---
# 生产配置
server
  port: 8082
spring:
  application:
    name:  microservicecloud-config-prod
  profiles: prod
